apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "form-submission-validation.fullname" . }}-test-connection"
  labels:
    {{- include "form-submission-validation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "form-submission-validation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
